<!DOCTYPE html>
<html>
	<head>
  	<title><%= title %></title>
  	<%= render 'shared/stylesheets' %>
  	<%= javascript_include_tag :defaults %>
  	<%= csrf_meta_tag %>
	</head>
	<body>
		<% flash.each do |key, value| %>
				<div class="flash no-pull"><%= raw(value) %></div>
			<% end %>
		<div id="main">
			<%= yield %>
			<%= debug(params) if Rails.env.development? %>
		</div>
		<%= render 'shared/footer' %>
	</body>
</html>
